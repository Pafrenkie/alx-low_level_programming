#include <stdio.h>
/**
 * main -Prog  Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int d;
char c;

for (d = 0; d < 10; d++)
{
putchar(d + '0');
d++;
}

while

(c <= 'f')
{
putchar(c);
c++;
}
putchar('\n');
return (0);
}
